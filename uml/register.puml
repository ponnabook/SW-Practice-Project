@startuml Register (POST)

header Co-working Space Reservation Sequence Diagram
footer Page %page% of %lastpage%
title "Register (POST)"

participant "User" as client
participant "<<javaScript>>\n:server" as server
participant "<<router>>\n:auth" as routerAuth
participant "<<controllers>>\n:auth" as controllersAuth
participant "<<model>>\n:User" as modelUser
database "<<MongoDB>>\n:user" as userDatabase

client->server ++:req.post('/coworkingSpaces')
server->routerAuth ++:app.use('/coworkingSpaces',coworkingSpaces)
routerAuth -> controllersAuth ++:getCoworkingSpaces()
controllersAuth->modelUser ++:find()
modelUser -> userDatabase ++: coworkingSpaceSchema
userDatabase --> modelUser --: coworkingSpaces
controllersAuth <-- modelUser --:coworkingSpaces
controllersAuth ->client --:response

@enduml